
#include <iostream>
#include <memory.h>

int R, C;	// max row, max col
int dir[3] = {1, 0, -1};
bool board[10000][500];

bool inRange(int y, int x)
{

}

int main()
{
	std::ios_base::sync_with_stdio(false);
	std::cin.tie(nullptr);
	std::cout.tie(nullptr);
	memset(board, 0, sizeof(board));

	std::cin >> Y >> X;
}
