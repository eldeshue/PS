
#include <iostream>
#include <vector>

/*
	길이가 N이고 사전순으로 K번째에 위치한 unbalanced binary string 구하기

	UbStr은 0과 1로 구성되며, 왼쪽부터 accumulate했을 때, 1의 개수가 절반보다 많아서는 안된다.

	길이가 N이면 항상 UB임. 
*/

int main()
{
	std::ios_base::sync_with_stdio(false);
	std::cin.tie(nullptr);
	std::cout.tie(nullptr);
}
