
#include <iostream>

typedef struct Node
{
  int value;

  Node *parent = nullptr;
  Node *left = nullptr;
  Node *right = nullptr;

} Node;

class B_tree
{
};

int main()
{
  std::ios_base::sync_with_stdio(false);
  std::cin.tie(NULL);
}